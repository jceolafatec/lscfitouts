# Portfolio Manager PRO Dependencies
# Install with: pip install -r requirements.txt

# No external dependencies needed! 
# project_manager_pro.py uses only Python standard library:
# - tkinter (built-in)
# - json (built-in)
# - xml.etree.ElementTree (built-in)
# - shutil (built-in)
# - pathlib (built-in)
# - os (built-in)

# Optional: PyInstaller to package as .exe
# pyinstaller>=5.0

# Optional: Pillow if you want to add image processing later
# pillow>=9.0

# Optional: watchdog for instant folder/file change detection in
# sync_portfolio_assets.py watch mode.
# watchdog>=4.0
